// import createElizaGoatPlugin from "./elizaos";

const character = {
    name: "GOAT Agent",
    description: "An agent for onchain actions",
    // TODO: Fix issue with importing plugin
    // plugins: [createElizaGoatPlugin()],
    // Add other character configurations as needed
};

export default function startAgent() {
    console.log("TODO: Deploy the agent here\nCharacter: ", character);
}

startAgent();
